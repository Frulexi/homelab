[homelab]
owl_cloud  ansible_host=192.168.1.10  ansible_port=1022
# new_server is kept alongside owl_cloud during migration; remove owl_cloud once cutover is complete.
# ansible_port starts at 22; update to 1022 after hardening playbook runs.
new_server ansible_host=192.168.1.191 ansible_port=22 ansible_ssh_private_key_file=~/.ssh/<new_server_key>

[homelab:vars]
ansible_user=frulexi
# Update this to the location of the SSH key on the control node; keep the real path out of version control.
ansible_ssh_private_key_file=/Users/frulexi/tunnel/owl-cloud0

